models:
  - name: large_payment
    description: Trigger for tracking large payments
    config:
        materialized: trigger
        handler: metric_handler
  - name: small_payment
    description: Trigger for tracking small payments
    config:
        materialized: trigger
        handler: metric_handler
